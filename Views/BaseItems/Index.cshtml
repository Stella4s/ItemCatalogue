@model BaseItemIndexViewModel

@{
    ViewData["Title"] = "Index";
}

<div class="container">
    <div class="row">
        <div class="col-sm-12 col-md-9">

            <div class="mb-1">
                <a asp-action="Create">Create New</a>

                <div class="d-inline-flex">
                    @if (Model.SelectedBaseItem != null)
                    {
                        <partial name="_ItemToInventoryButtons" model="@Model.SelectedBaseItem.BaseItemID" />
                    }
                </div>

            </div>

        </div>
    </div>

    <div class="row">    
        <div class="col-9">

           <!-- Previous Table solution
            <div class="row">
                <table class="table table-catalogue">
                    <thead>
                        <tr>
                            <th></th>
                            <th>
                                @Html.DisplayNameFor(model => model.BaseItems.FirstOrDefault().Name)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.BaseItems.FirstOrDefault().BasePrice)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.BaseItems.FirstOrDefault().Description)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.BaseItems.FirstOrDefault().MainCategory)
                            </th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>

                        @foreach (var item in Model.BaseItems)
                        {
                            string selectedRow = "";
                            if (item.BaseItemID == (int?)ViewData["SelectedID"])
                            {
                                selectedRow = "table-success";
                            }
                            <tr class="@selectedRow" asp-action="Index" asp-route-id="@item.BaseItemID">
                                <td>
                                    <img src="@Url.Content("~/Images/" + item.ImageUrl)" />
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Name)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.BasePrice)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Description)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.MainCategory.Name)
                                </td>
                                <td>
                                    <a asp-action="Index" asp-route-id="@item.BaseItemID">Select</a> |
                                    <a asp-action="Edit" asp-route-id="@item.BaseItemID">Edit</a> |
                                    <a asp-action="Details" asp-route-id="@item.BaseItemID">Details</a> |
                                    <a asp-action="Delete" asp-route-id="@item.BaseItemID">Delete</a>
                                </td>
                            </tr>
                        }
                    </tbody>

                </table>
            </div>
            -->

           <div class="list-group list-group-flush mimic-table table">
                    <div class="mimic-headergroup">
                        <div class="mimic-row">
                            <div></div>
                            <div>
                                @Html.DisplayNameFor(model => model.BaseItems.FirstOrDefault().Name)
                            </div>
                            <div>
                                @Html.DisplayNameFor(model => model.BaseItems.FirstOrDefault().BasePrice)
                            </div>
                            <div>
                                @Html.DisplayNameFor(model => model.BaseItems.FirstOrDefault().Description)
                            </div>
                            <div>
                                @Html.DisplayNameFor(model => model.BaseItems.FirstOrDefault().MainCategory)
                            </div>
                        </div>
                    </div>
                    <div class="mimic-rowgroup">
                        @foreach (var item in Model.BaseItems)
                        {
                            string selectedRow = "";
                            if (Model.SelectedBaseItem != null)
                            {
                                if (item.BaseItemID == (int?)Model.SelectedBaseItem.BaseItemID)
                                {
                                    selectedRow = "active";
                                }
                            }
                        <a asp-action="Index" asp-route-id="@item.BaseItemID" class="list-group-item list-group-item-action mimic-row @selectedRow">
                            <div>  <img src="@Url.Content("~/Images/" + item.ImageUrl)" /></div>
                            <div> @Html.DisplayFor(modelItem => item.Name)</div>
                            <div> @Html.DisplayFor(modelItem => item.BasePrice)</div>
                            <div> @Html.DisplayFor(modelItem => item.Description)</div>
                            <div> @Html.DisplayFor(modelItem => item.MainCategory.Name)</div>
                        </a>
                        }
                    </div>
                </div>

        </div>

        <div class="col-3">
            <div class="col">
                @if (Model.SelectedBaseItem != null)
                {
                    <partial name="_ItemDetailCard" model="@Model.SelectedBaseItem" />
                }
            </div>
            <div class="w-100"></div>
            <div class="col">
                @if (Model.SelectedBaseItem != null)
                {
                    <partial name="_ItemPageLinks" model="@Model.SelectedBaseItem.BaseItemID"/>
                }
            </div>

        </div>
    </div>
</div>